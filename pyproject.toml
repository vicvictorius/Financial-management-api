[tool.ruff]
line-length = 88
target-version = "py314"

exclude = [
    ".git",
    ".venv",
    "__pycache__",
    "alembic/versions",
]

[tool.ruff.lint]
select = [
    "E",
    "F",
    "I",
    "B",
]

ignore = [
    "B008",
]

[tool.ruff.lint.isort]
known-first-party = ["app"]
