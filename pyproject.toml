[tool.ruff]
target-version = "py314"
line-length = 100

[tool.ruff.lint]
select = [
    "E",
    "W",
    "F",
    "I",
]

ignore = [
    "B008",
]

exclude = [
    ".git",
    ".venv",
    "__pycache__",
    ".pytest_cache",
    "alembic/versions",
]